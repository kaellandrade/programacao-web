version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    environment:
      - POSTGRES_HOST=progweb.cf5sarg82ulb.us-east-1.rds.amazonaws.com
      - POSTGRES_PORT=5432
      - POSTGRES_USER=progweb
      - POSTGRES_PASSWORD=progweb123
      - POSTGRES_DB=progweb
